To install "LinuxSystemMonitor" systemwide, simply use -i (install)

./system_monitor.sh -i 

Once installed, you can use the command 'monitor' in any directory and it will run.

Otherwise, just run the shell script like normal.
